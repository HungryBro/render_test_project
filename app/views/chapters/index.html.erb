<h1>Chapters for <%= @novel.title %></h1>

<%= link_to "Add New Chapter", new_novel_chapter_path(@novel), class: "btn btn-success" %>

<ul>
  <% @chapters.each do |chapter| %>
    <li>
      <%= link_to chapter.title, novel_chapter_path(@novel, chapter) %>
    </li>
  <% end %>
</ul>

<%= link_to "Back to Novel", novel_path(@novel), class: "btn btn-secondary" %>
